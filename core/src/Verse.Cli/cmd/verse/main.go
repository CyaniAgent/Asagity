package main

// `verse` CLI entrypoint.
// Subcommands: start, stop, restart, status, app (list/enable/disable).

func main() {
	// TODO: dispatch verse subcommands via internal/runner, doctor, modulemgr.
}
